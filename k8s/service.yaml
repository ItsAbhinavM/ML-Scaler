apiVersion: v1
kind: Service
metadata:
  name: ml-scaler-service
  labels:
    app: ml-scaler
spec:
  type: NodePort
  selector:
    app: ml-scaler
  ports:
    - protocol: TCP
      name: http
      port: 8000
      targetPort: 8000
      nodePort: 30081
